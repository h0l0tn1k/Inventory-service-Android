version: '2'
services:
  assessment-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/assessment/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=assessment
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
