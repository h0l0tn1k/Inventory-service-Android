version: '2'
services:
  riskframework-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/riskFramework/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=riskFramework
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
